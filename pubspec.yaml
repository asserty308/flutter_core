name: flutter_core
description: "A new Flutter package project."
version: 1.0.7
homepage:

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^3.0.3
  intl: ^0.20.2
  logger: ^2.6.2

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter

flutter:
