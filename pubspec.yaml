name: flutter_core
description: "A new Flutter package project."
version: 2.0.0
homepage:

environment:
  sdk: ^3.13.0

dependencies:
  cupertino_ui: ^1.0.0
  flutter:
    sdk: flutter
  flutter_riverpod: ^3.0.3
  intl: ^0.20.2
  logger: ^2.6.2
  material_ui: ^1.0.1

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter

flutter:
